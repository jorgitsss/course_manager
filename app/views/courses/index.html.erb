<p style="color: green"><%= notice %></p>
<h1>Courses</h1>
<%= link_to "New course", new_course_path %>
<hr>
<div id="courses">
  <% @courses.each do |course| %>
    <%= render course %>
    <p>
      <%= link_to "Show this course", course %> / 
      <%= link_to "Show all lessons of this course", [course] %>
    </p>
    <hr>
  <% end %>
</div>


